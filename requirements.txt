# Tested with Python 3.13.9 on Windows.
# Covers the repository's runnable workflow under `src/`:
# data export, baselines, PulsePPG/PaPaGei feature extraction,
# regression training, plotting, and notebook exploration.
#
# If you need GPU acceleration, replace the `torch` line below
# with the CUDA wheel that matches your machine.

numpy==2.3.5
pandas==2.3.3
scipy==1.16.3
scikit-learn==1.7.2
matplotlib==3.10.6
joblib==1.5.2
torch==2.10.0
tqdm==4.67.1
jupyter==1.1.1
